/*
 * There are 5 instances of displayType listed below.
 * These 5 displayType objects are references as:
 * DisplayType.LED
 * DisplayType.OLED
 * ...
 */

enum DisplayType {
    LED, OLED, PLASMA, LCD, CRT
}